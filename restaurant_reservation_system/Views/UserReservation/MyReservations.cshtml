@using restaurant_reservation.Models
@using restaurant_reservation_system.Models.ViewModel
@{
    ViewData["Title"] = "My Reservations";
}

@model List<MyReservationsViewModel>

<div class="container mt-4">
    <h2 class="text-center mb-4">My Reservations</h2>

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        @{
            int i = 1;
        }
        @foreach (var reservation in Model)
        {
            <div class="col">
                <div class="card h-100">
                    <div class="card-header bg-primary text-white">
                        <div class="d-flex justify-content-between align-items-center">
                            @if (reservation.Status == ReservationStatus.Confirmed.ToString())
                            {
                                <span class="badge bg-success">@reservation.Status</span>
                            }
                            else if(reservation.Status == ReservationStatus.Pending.ToString())
                            {
                                <span class="badge bg-warning">@reservation.Status</span>
                            }
                            else if(reservation.Status == ReservationStatus.Outdated.ToString())
                            {
                                <span class="badge bg-danger">@reservation.Status</span>
                            }
                        </div>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">Reservation #@i</h5>

                        <div class="mb-3">
                            <i class="bi bi-calendar-date"></i>
                            <span class="ms-2">@reservation.ReservationDateLocal.ToShortDateString()</span>
                        </div>
                        <div class="mb-3">
                            <i class="bi bi-clock"></i>
                            <span class="ms-2">@reservation.ReservationHour:00</span>
                        </div>
                        <div class="mb-3">
                            <i class="bi bi-people"></i>
                            <span class="ms-2">@reservation.NumberOfGuests guests</span>
                        </div>
                        <div class="text-muted small">
                            <i class="bi bi-calendar-check"></i>
                            <span class="ms-2">Created: @reservation.CreatedAtLocal.ToShortDateString() @reservation.CreatedAtLocal.ToShortTimeString()</span>
                        </div>

                        <br />
                        <button href="#" class="btn-outline-primary btn-sm">Cancel</button>
                    </div>
                </div>
            </div>

            i++;
        }
    </div>
</div>


<style >
    .card {
        transition: transform 0.2s;
        cursor: pointer;
    }

    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
    }
</style>